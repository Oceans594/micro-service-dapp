syntax = "proto3";

package service;

// 定义请求
message LoginRequest {
    string username = 1;
    string password = 2;
}

// 定义响应
message LoginResponse {
    string token = 1;
    string refresh = 2;
}